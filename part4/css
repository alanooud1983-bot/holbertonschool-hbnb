body {
    font-family: Arial, sans-serif;
    margin: 0;
}

header, footer {
    padding: 15px;
    background: #f5f5f5;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    height: 50px;
}

.login-button {
    padding: 8px 15px;
    background: #0077cc;
    color: white;
    border-radius: 5px;
    text-decoration: none;
}

.place-card, .review-card, .place-details {
    margin: 20px;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 10px;
}

.details-button {
    padding: 8px 12px;
    background: #28a745;
    color: white;
    border-radius: 5px;
    text-decoration: none;
}

form {
    margin: 20px;
    display: flex;
    flex-direction: column;
    gap: 10px;
}
